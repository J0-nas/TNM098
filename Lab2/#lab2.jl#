println("Hello world")

function parseInput()
	 file = EachLine(open("EyeTrack-raw.tsv"))
	 res = []
	 for line in file
	     println(split(line, '\t'))
	     #println(typeof(split(line, '\t')))
	 end
	 #println(res[0])
	 return res
end

parseInput()