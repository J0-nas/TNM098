#include <iterator>
#include <ifstream>
#include <iostream>

using namespace std;

int main(int argc, char* argv) {
  ifstream ifs {argv[1]};
  string line {};
  getline(ifs, line);
  cout << line << endl;
}
